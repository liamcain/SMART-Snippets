###scope:text.plain
Hello ${0:Hellozzz}, ${1:AC{a:word,nextword,anotherword} } } `insert('hi')`
SMART Snippets allow for Python inside your snippets. AC{auto:first,second,third} There is still much to do.
First, I need to `insert('add')` scopes for the plugin.