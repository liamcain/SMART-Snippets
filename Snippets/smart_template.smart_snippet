###regex:no
###trigger:smart_template
###scope:source.smart_snippet
```insert('###')```Should the trigger be matched with regex or not?
```insert('###')```If you don't know what that means, set it to no.
```insert('###')```regex: ${1:QP{yes or no:yes,no}}
```insert('###')```trigger: ${2:--}
${3:###scope:} ${4:text.html}
${0:}